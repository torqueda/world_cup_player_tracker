# Python dependencies for the data pipeline scripts in scripts/.
# Install into the project virtualenv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
beautifulsoup4>=4.15
openpyxl>=3.1
pandas>=3.0
python-dateutil>=2.9
requests>=2.34
