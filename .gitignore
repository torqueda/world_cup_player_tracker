# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node / frontend (site/ has its own .gitignore too)
node_modules/
site/dist/
.pnpm-store/

# Editor / temp
*.swp
~$*
.tmp/

# Timestamped workbook backups (git history is the real backup now)
data/master/*.backup_*.xlsx
